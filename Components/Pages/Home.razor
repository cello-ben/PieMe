@page "/"
@using Microsoft.AspNetCore.Html

<PageTitle>Home</PageTitle>

<h1>Want some pie?</h1>

<p>One popular use case for Blazor is as a tool for creating internal dashboards. That’s the kind of situation where charts could come in very handy. As an exercise, I set about writing my own charting logic. Take a look!</p>
@{ Dictionary<string, double> d = new(); d.Add("Rent", 69.42); d.Add("Utilities", 30.54); }
<Chart percentages=d></Chart>

<h3>Usage</h3>
<code>
    @foreach (string line in lines)
    {
        @line <br>
    }
</code>

@code
{
    string[] lines = {"Dictionary<string, double> percentages = new();", "percentages.Add(“Rent”, 37.3);", "percentages.Add(“Utilities”, 12.4)", "percentages.Add(Payroll, 50.3);", "<Chart percentages=percentages></Chart>"};
}